### macOS
# Finder metadata
.DS_Store

# Thumbnails
._*

# Custom folder icons
Icon


# Volume root files
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.mcp.json
.envrc

# iOS build-time secrets (populated from .envrc locally, from CI secrets in CI).
ios/slabbist/Config/Secrets.xcconfig

# Git worktrees (isolated workspaces for parallel feature work)
.worktrees/
.agents

# Supabase Edge Functions auto-generated
supabase/functions/**/deno.lock

.claude
.impeccable.md